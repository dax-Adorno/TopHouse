# Frontend de TopHouse

Aplicación pública construida con React, TypeScript y Vite.

## Desarrollo

```bash
npm install
npm run dev
```

Copiar `.env.example` como `.env.local` para cambiar la URL del backend.

## Controles de calidad

```bash
npm run typecheck
npm run lint
npm run test
npm run build
```
